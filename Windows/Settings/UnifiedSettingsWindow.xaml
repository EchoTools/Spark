<Window x:Class="Spark.UnifiedSettingsWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:l="clr-namespace:Spark"
		xmlns:p="clr-namespace:Spark.Properties"
		mc:Ignorable="d"
		Title="{x:Static p:Resources.IgniteBot___Settings}"
		Height="700" Width="610"
		Top="{l:SettingBinding settingsWindowTop}"
		Left="{l:SettingBinding settingsWindowLeft}"
		Background="{DynamicResource SurfaceGround}"
		Loaded="WindowLoad"
		FontSize="14"
		Padding="10,10,10,10"
		ResizeMode="CanResize"
		SizeToContent="Manual"
		Icon="/img/ignite_logo.ico"
		WindowStartupLocation="Manual"
		DataContext="{Binding RelativeSource={RelativeSource Self}}"
		>
	<Window.Resources>
		<l:EchoVRSettingsManager x:Key="es"/>

		<!-- ══════════════ Shared settings-tab styles ══════════════ -->
		<Style x:Key="CardHeading" TargetType="TextBlock">
			<Setter Property="FontSize" Value="11.5"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Foreground" Value="{DynamicResource TextFaint}"/>
			<Setter Property="Margin" Value="2,0,0,10"/>
		</Style>

		<Style x:Key="SettingsCard" TargetType="Border">
			<Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="8"/>
			<Setter Property="Padding" Value="14"/>
			<Setter Property="Margin" Value="0,0,0,14"/>
		</Style>

		<Style x:Key="ThemedCheckBox" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="FontSize" Value="12.5"/>
			<Setter Property="Margin" Value="0,0,0,9"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
		</Style>

		<Style x:Key="ThemedComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
			<Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
			<Setter Property="Padding" Value="8,4"/>
		</Style>

		<Style x:Key="FieldLabel" TargetType="Label">
			<Setter Property="Foreground" Value="{DynamicResource TextDim}"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Padding" Value="0,4,8,4"/>
		</Style>

		<Style x:Key="ThemedTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
			<Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Padding" Value="8,6"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
		</Style>

		<Style x:Key="CardExpander" TargetType="Expander">
			<Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="14"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Margin" Value="0,0,0,14"/>
		</Style>

		<Style x:Key="InnerCard" TargetType="Border">
			<Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="7"/>
			<Setter Property="Padding" Value="12"/>
			<Setter Property="Margin" Value="0,0,0,12"/>
		</Style>

		<Style x:Key="SliderRowLabel" TargetType="Label">
			<Setter Property="Foreground" Value="{DynamicResource TextDim}"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Width" Value="150"/>
			<Setter Property="Padding" Value="0,4,8,4"/>
		</Style>

		<Style x:Key="SliderValueBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
			<Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Height" Value="24"/>
			<Setter Property="Width" Value="44"/>
			<Setter Property="TextAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Margin" Value="6,0,4,0"/>
		</Style>

		<Style x:Key="UnitLabel" TargetType="Label">
			<Setter Property="Foreground" Value="{DynamicResource TextFaint}"/>
			<Setter Property="FontSize" Value="11.5"/>
			<Setter Property="Padding" Value="0,4,0,4"/>
		</Style>

		<Style x:Key="HintText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="{DynamicResource TextFaint}"/>
			<Setter Property="FontSize" Value="11"/>
			<Setter Property="Margin" Value="0,0,0,10"/>
		</Style>

		<Style x:Key="AccentButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="{DynamicResource ControlAccent}"/>
			<Setter Property="Foreground" Value="{DynamicResource ControlAccentForeground}"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="{DynamicResource ControlBrightDefaultBackground}"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="SecondaryButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Padding" Value="12,7"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="{DynamicResource ControlMouseOverBackground}"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="52" />
			<RowDefinition />
			<RowDefinition Height="50" />
		</Grid.RowDefinitions>
		<Label x:Name="label" Content="{x:Static p:Resources.Settings}" FontSize="20" Margin="10,10,10,0" />

		<TabControl x:Name="tabControl" SelectedIndex="0" Grid.Row="1">

			<TabItem Header="{x:Static p:Resources.General}" Padding="8,6,8,6">
				<ScrollViewer VerticalScrollBarVisibility="Auto">
					<StackPanel Margin="14,14,14,14">

						<StackPanel Orientation="Horizontal" Margin="0,0,0,14">
							<Button Content="{x:Static p:Resources.Show_first_time_setup_window}"
							        Style="{StaticResource SecondaryButtonStyle}"
							        Margin="0,0,10,0" HorizontalAlignment="Left" Width="207"
							        Click="ShowFirstTimeSetupWindowClicked" />
							<Button Content="{x:Static p:Resources.Open_settings_folder}"
							        Style="{StaticResource SecondaryButtonStyle}"
							        HorizontalAlignment="Left" Width="207"
							        Click="OpenSettingsFileFolder" />
						</StackPanel>

						<!-- Connection -->
						<TextBlock Text="CONNECTION" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<TextBlock x:Name="ThisPCLocalIP" Margin="0,0,0,10" FontSize="11.5" Foreground="{DynamicResource TextFaint}">---</TextBlock>
								<StackPanel Orientation="Horizontal">
									<Label Content="{x:Static p:Resources.EchoVR_IP__for_Quest_}" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
									<Grid Width="160">
										<TextBox x:Name="EchoVRIPTextBox" Text="{l:SettingLoad echoVRIP}" TextWrapping="NoWrap" HorizontalAlignment="Stretch"
										         Style="{StaticResource ThemedTextBox}" Margin="0,0,4,0" FontFamily="Consolas" Height="32"
										         VerticalAlignment="Center" TextChanged="EchoVRIPChanged" />
										<Button x:Name="SaveQuestIPButton" Click="SaveQuestIPClicked" Style="{StaticResource SecondaryButtonStyle}"
										        Visibility="Collapsed" HorizontalAlignment="Right" Padding="8,2" Content="{x:Static p:Resources.Save}"/>
									</Grid>
									<TextBox x:Name="EchoVRPortTextBox" Text="{l:SettingLoad echoVRPort}" TextWrapping="NoWrap" Width="60"
									         Style="{StaticResource ThemedTextBox}" Margin="0,0,8,0" FontFamily="Consolas" Height="32"
									         VerticalAlignment="Center" TextChanged="EchoVRPortChanged" />
									<Button x:Name="ResetIP" Content="{x:Static p:Resources.Local_PC}" Style="{StaticResource SecondaryButtonStyle}"
									        Margin="0,0,8,0" Padding="10,6" Click="ResetIP_Click" />
									<Button x:Name="FindQuest" Content="{x:Static p:Resources.Find_Quest_IP}" Style="{StaticResource SecondaryButtonStyle}"
									        Padding="10,6" Click="FindQuestClick" />
								</StackPanel>
								<Label x:Name="FindQuestStatusLabel" Visibility="Hidden" Content="{x:Static p:Resources.Searching_for_Quest}"
								       Foreground="{DynamicResource TextFaint}" Margin="0,4,0,0" FontSize="10" FontStyle="Italic" />
								<Button Content="Simple Spectate Mode" Style="{StaticResource SecondaryButtonStyle}"
								        Margin="0,10,0,0" HorizontalAlignment="Left"
								        Click="OpenQuestSpectatorPopup"
								        ToolTip="Open Simple Spectate Mode, which follows another player with a spectator client on this PC."/>
							</StackPanel>
						</Border>

						<!-- EchoVR Executable -->
						<TextBlock Text="ECHOVR EXECUTABLE" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<Label x:Name="ExeLocationLabel" Content="{x:Static p:Resources.EchoVR_Executable_Location_}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<TextBox Text="{l:SettingLoad echoVRPath}" TextWrapping="NoWrap"
								         Style="{StaticResource ThemedTextBox}"
								         HorizontalAlignment="Stretch"
								         Margin="0,0,0,10" TextChanged="ExecutableLocationChanged"
								         MaxLines="1" />
								<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
									<ProgressBar x:Name="ReshadeProgress" Visibility="Collapsed" Width="200" Margin="0,0,10,0"></ProgressBar>
									<Button Content="{x:Static p:Resources.Install_ReShade}" Style="{StaticResource SecondaryButtonStyle}" Click="InstallReshade" Margin="0,0,10,0"></Button>
									<Button Content="{x:Static p:Resources.Remove_ReShade}" Style="{StaticResource SecondaryButtonStyle}" Click="RemoveReshade"></Button>
								</StackPanel>
							</StackPanel>
						</Border>

						<!-- App Behavior -->
						<TextBlock Text="APP BEHAVIOR" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
									<Label Content="{x:Static p:Resources.Language}" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
									<ComboBox Style="{StaticResource ThemedComboBox}" Margin="0,0,10,0" HorizontalAlignment="Left" Width="148" SelectedIndex="{l:SettingBinding languageIndex}" >
										<ComboBoxItem>English</ComboBoxItem>
										<ComboBoxItem>æ—¥æœ¬èªž</ComboBoxItem>
									</ComboBox>
									<TextBlock Text="{x:Static p:Resources._Requires_Restart_}" Foreground="{DynamicResource TextFaint}" FontSize="11" VerticalAlignment="Center"/>
								</StackPanel>
								<!-- Redundant now that the Change Theme tab offers full colour customization.
								<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
									<Label Content="Interface Theme" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
									<ComboBox Style="{StaticResource ThemedComboBox}" SelectedIndex="{Binding SetTheme}" Width="56">
										<ComboBoxItem>
											<Rectangle Width="20" Height="20" Fill="Yellow"/>
										</ComboBoxItem>
										<ComboBoxItem>
											<Rectangle Width="20" Height="20" Fill="Orange"/>
										</ComboBoxItem>
										<ComboBoxItem>
											<Rectangle Width="20" Height="20" Fill="Red"/>
										</ComboBoxItem>
									</ComboBox>
								</StackPanel>
								-->
								<CheckBox x:Name="startWithWindowsCheckbox" Content="{x:Static p:Resources.Start_with_Windows}"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{Binding StartWithWindows}" HorizontalAlignment="Left" />
								<CheckBox x:Name="startMinimizedCheckbox" Content="{x:Static p:Resources.Start_Minimized}"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding startMinimized}" HorizontalAlignment="Left" />
								<CheckBox x:Name="closeButtonExitsAppCheckbox" Content="Closing Spark exits the app instead of asking to hide"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding closeButtonExitsApp}" HorizontalAlignment="Left"
											  ToolTip="When enabled, clicking the close button quits Spark immediately instead of showing the 'Are you sure?' hide/exit prompt." />
								<CheckBox x:Name="enableBetasCheckbox" Content="{x:Static p:Resources.Get_Beta_Updates_to_Spark}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding betaUpdates}" ToolTip="{x:Static p:Resources.betaUpdates_ToolTip}"/>
								<CheckBox x:Name="capturevp2Checkbox" Content="{x:Static p:Resources.Enable__capturevp2_when_launching_spectator}"
								          Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{l:SettingBinding capturevp2}" HorizontalAlignment="Left" />
								<CheckBox Content="Enable -capturevp2 when launching in VR (not recommended)"
								          Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{l:SettingBinding capturevp2VR}" HorizontalAlignment="Left" />
								<CheckBox x:Name="remoteLoggingCheckbox" Content="Send Logs to Ignite"
								          Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{l:SettingBinding logToServer}" HorizontalAlignment="Left" Visibility="Collapsed" />
								<CheckBox x:Name="LowFrequencyModeCheckBox" Content="{x:Static p:Resources.Low_Frequency_Mode}" ToolTip="Queries the API less frequently if you are having performance problems."
								          Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{l:SettingBinding lowFrequencyMode}" HorizontalAlignment="Left" />
								<CheckBox x:Name="enableAutoFocusCheckbox" Content="{x:Static p:Resources.Autofocus_EchoVR}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding isAutofocusEnabled}" ToolTip="{x:Static p:Resources.enableAutoFocusCheckbox_ToolTip}"/>
								<CheckBox x:Name="shareGlobalPlaytimeCheckbox" Content="Contribute my playtime to the community total and leaderboard"
								          Style="{StaticResource ThemedCheckBox}" Margin="0"
								          IsChecked="{l:SettingBinding shareGlobalPlaytime}" HorizontalAlignment="Left"
								          ToolTip="Adds your hours to the 'Everyone on Spark' figure on the Session card, and puts your name on the public playtime leaderboard.&#10;Sends your Discord name — or your EchoVR name if you're not logged in to Discord — plus an anonymous per-install ID and a seconds count, once a minute. No match data.&#10;The community total still shows when this is off." />
							</StackPanel>
						</Border>

						<!-- Dashboard Tabs -->
						<TextBlock Text="DASHBOARD TABS" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<TextBlock Text="Which tabs are visible on the main screen" Foreground="{DynamicResource TextDim}" FontSize="11.5" Margin="0,0,0,10"/>
								<WrapPanel Orientation="Horizontal">
									<CheckBox Content="Dashboard" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showDashboardTab}" Margin="0,0,18,9" />
									<CheckBox Content="Portal" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showPortalTab}" Margin="0,0,18,9" />
									<CheckBox Content="Web Browser" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showWebBrowserTab}" Margin="0,0,18,9" />
									<CheckBox Content="Download EchoVR" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showDownloadEchoVRTab}" Margin="0,0,18,9" />
									<CheckBox Content="Atlas" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showLinksTab}" Margin="0,0,18,9" />
									<CheckBox Content="Event Log" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showEventLogTab}" Margin="0,0,18,9" />
									<CheckBox Content="Scoreboard" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showScoreboardTab}" Margin="0,0,18,9" />
									<CheckBox Content="Speaker System" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showSpeakerSystemTab}" Margin="0,0,18,9" />
									<CheckBox Content="Server Info" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showServerInfoTab}" Margin="0,0,18,9" />
									<CheckBox Content="Camera Write" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showWriteAPITab}" Margin="0,0,18,9" />
									<CheckBox Content="Echo GP" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showEchoGPTab}" Margin="0,0,18,9" />
									<CheckBox Content="Match Rules" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showPrivateMatchRulesTab}" Margin="0,0,18,9" />
									<CheckBox Content="Choose Server" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showCreateServerTab}" Margin="0,0,18,9" />
									<CheckBox Content="PlayerCard" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showPlayerCardTab}" Margin="0,0,18,9" />
									<CheckBox Content="Friends" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding showFriendsTab}" Margin="0,0,18,9" />
								</WrapPanel>
							</StackPanel>
						</Border>

						<!-- Spectating -->
						<TextBlock Text="SPECTATING" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<TextBlock Text="Spectatorstream (spectates the highest level public match)" Foreground="{DynamicResource TextDim}" FontSize="11.5" Margin="0,0,0,8"/>
								<CheckBox Content="{x:Static p:Resources.Use_anonymous_mode_for_Spectatorstream}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding spectatorStreamNoOVR}" ToolTip="Your username will show up as anonymous when launching the spectatorstream. This also allows you to have two instances of EchoVR open at once."/>
								<CheckBox x:Name="autorestartCheckbox" Content="{x:Static p:Resources.Autorestart_EchoVR_into_autospectator}"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding autoRestart}" HorizontalAlignment="Left"
											  ToolTip="{x:Static p:Resources.Automatically_restarts_spectatorstream_when_it_crashes}" />

								<Border Height="1" Background="{DynamicResource SurfaceBorderSoft}" Margin="0,4,0,12"/>

								<TextBlock Text="{x:Static p:Resources.Spectate_Me__Opens_the_spectator_for_your_current_match_}" Foreground="{DynamicResource TextDim}" FontSize="11.5" Margin="0,0,0,8"/>
								<CheckBox Content="{x:Static p:Resources.Allow_Spectate_Me_on_local_PC}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding allowSpectateMeOnLocalPC}" ToolTip="Enables the Spectate Me function when your IP is 127.0.0.1. This is not recommended and is still experimental."/>
								<CheckBox Content="{x:Static p:Resources.Use_anonymous_mode_in_Spectate_Me}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding useAnonymousSpectateMe}" ToolTip="Enables the anonymous mode for the Spectate Me function. Uncheck this to use a second Oculus account for your spectator."/>
								<CheckBox Content="{x:Static p:Resources.Spectate_Me_on_by_default}" Style="{StaticResource ThemedCheckBox}" Margin="0" IsChecked="{l:SettingBinding spectateMeOnByDefault}" ToolTip="Automatically enables the Spectate Me feature when Spark starts."/>
							</StackPanel>
						</Border>

						<!-- Discord -->
						<TextBlock Text="DISCORD" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<CheckBox x:Name="discordRichPresenceCheckbox" Content="{x:Static p:Resources.Discord_Rich_Presence}"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding discordRichPresence}" HorizontalAlignment="Left" />
								<CheckBox x:Name="discordRichPresenceServerLocationCheckbox" Content="{x:Static p:Resources.Show_Server_Location_in_Discord_RP_status}"
											  Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding discordRichPresenceServerLocation}" HorizontalAlignment="Left" />
								<CheckBox x:Name="discordRichPresenceSpectatorCheckbox" Content="{x:Static p:Resources.Discord_Rich_Presence_Spectator}"
											  Style="{StaticResource ThemedCheckBox}" Margin="0"
											  IsChecked="{l:SettingBinding discordRichPresenceSpectator}" HorizontalAlignment="Left" />
							</StackPanel>
						</Border>

						<!-- Spectator Camera Settings -->
						<TextBlock Text="SPECTATOR CAMERA" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<TextBlock Text="{x:Static p:Resources.CameraSettingsDescription}" Foreground="{DynamicResource TextDim}" FontSize="11.5" TextWrapping="Wrap" Margin="0,0,0,12"></TextBlock>
								<CheckBox x:Name="HideUICheckbox" Content="{x:Static p:Resources.Hide_EchoVR_UI}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding hideEchoVRUI}" HorizontalAlignment="Left" Click="HideEchoVRUINow"/>
								<CheckBox Content="{x:Static p:Resources.Toggle_Minimap_after_Goals}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding toggleMinimapAfterGoals}" HorizontalAlignment="Left" />
								<CheckBox x:Name="HideMinimapCheckbox" Content="{x:Static p:Resources.Don_t_Show_Minimap__M_}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding alwaysHideMinimap}" HorizontalAlignment="Left" Click="HideMinimapNow" />
								<Label Content="Mute player comms:" Style="{StaticResource FieldLabel}" Margin="0,4,0,4"/>
								<ComboBox x:Name="MutePlayerCommsDropdown" Style="{StaticResource ThemedComboBox}" Margin="0,0,0,10" HorizontalAlignment="Left" Width="300" SelectionChanged="MutePlayerCommsDropdownChanged">
									<ComboBoxItem Content="{x:Static p:Resources.Don_t_Change}"/>
									<ComboBoxItem Content="{x:Static p:Resources.Mute_enemy_team}"/>
									<ComboBoxItem Content="{x:Static p:Resources.Mute_all_players}"/>
								</ComboBox>
								<CheckBox x:Name="HideNameplatesCheckbox" Content="{x:Static p:Resources.Hide_Player_Nameplates}" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding hideNameplates}" HorizontalAlignment="Left" Click="ToggleNameplatesNow"/>
								<Label Content="{x:Static p:Resources.Camera_Mode_}" Style="{StaticResource FieldLabel}" Margin="0,4,0,4"/>
								<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
									<ComboBox SelectedIndex="{l:SettingBinding spectatorCamera}" Style="{StaticResource ThemedComboBox}" SelectionChanged="CameraModeDropdownChanged" Margin="0,0,10,0" HorizontalAlignment="Left" Width="300">
										<ComboBoxItem Content="{x:Static p:Resources.Auto__leave_default_}"/>
										<ComboBoxItem Content="{x:Static p:Resources.Sideline}"/>
										<ComboBoxItem Content="{x:Static p:Resources.Follow_client_player___Spectate_Me__only_}"/>
										<ComboBoxItem Content="{x:Static p:Resources.Follow_specific_player}"/>
										<ComboBoxItem Content="Follow Discholder"/>
										<ComboBoxItem Content="GoPro (Hand camera)"/>
									</ComboBox>
									<Button x:Name="findCameraNowButton" Content="{x:Static p:Resources.Find_Now}" Style="{StaticResource SecondaryButtonStyle}" Click="SpectatorCameraFindNow" Padding="12,6"/>
								</StackPanel>
								<StackPanel x:Name="followSpecificPlayerPanel" Orientation="Horizontal" Margin="0,0,0,10">
									<Label Content="{x:Static p:Resources.Follow_specific_player_name_}" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
									<TextBox Text="{l:SettingBinding followPlayerName}" Style="{StaticResource ThemedTextBox}" Width="200" VerticalAlignment="Center"/>
								</StackPanel>
								<Label x:Name="followCameraModeLabel" Content="{x:Static p:Resources.Follow_client_player_camera_mode_}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<ComboBox x:Name="followCameraModeDropdown" SelectedIndex="{l:SettingBinding followPlayerCameraMode}" Style="{StaticResource ThemedComboBox}" Margin="0,0,0,10" HorizontalAlignment="Left" Width="300">
									<ComboBoxItem Content="{x:Static p:Resources.Follow}"/>
									<ComboBoxItem Content="{x:Static p:Resources.POV}"/>
								</ComboBox>
								<CheckBox x:Name="discholderFollowTeamCheckbox" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding discHolderFollowRestrictTeam}">Restrict to players on team</CheckBox>
								<Label x:Name="discholderFollowCameraModeLabel" Content="Discholder camera mode:" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<ComboBox x:Name="discholderFollowCameraModeDropdown" SelectedIndex="{l:SettingBinding discHolderFollowCamMode}" Style="{StaticResource ThemedComboBox}" Margin="0,0,0,10" HorizontalAlignment="Left" Width="300">
									<ComboBoxItem Content="{x:Static p:Resources.POV}"/>
									<ComboBoxItem Content="{x:Static p:Resources.Follow}"/>
								</ComboBox>
								<StackPanel x:Name="goProCameraPanel" Visibility="Collapsed">
									<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
										<Label Content="GoPro player name:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
										<TextBox Text="{l:SettingBinding goProPlayerName}" Style="{StaticResource ThemedTextBox}" Width="200" VerticalAlignment="Center"/>
									</StackPanel>
									<Label Content="Target Hand:" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
									<ComboBox SelectedIndex="{l:SettingBinding goProTargetHand}" Style="{StaticResource ThemedComboBox}" Margin="0,0,0,10" HorizontalAlignment="Left" Width="300">
										<ComboBoxItem Content="Left Hand"/>
										<ComboBoxItem Content="Right Hand"/>
									</ComboBox>
									<StackPanel Orientation="Horizontal">
										<Label Content="GoPro FOV (degrees):" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
										<Slider x:Name="goProFovSlider" Value="{l:SettingBinding goProWiderFov}" Minimum="40" Maximum="140" Width="150" Margin="4,0,8,0" Height="20" VerticalAlignment="Center"/>
										<TextBox Text="{Binding ElementName=goProFovSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ThemedTextBox}" Height="26" Width="44" VerticalAlignment="Center"/>
									</StackPanel>
								</StackPanel>
							</StackPanel>
						</Border>
					</StackPanel>
				</ScrollViewer>
			</TabItem>


			<TabItem Header="{x:Static p:Resources.Replays}" Padding="8,6,8,6">
				<ScrollViewer VerticalScrollBarVisibility="Auto">
					<StackPanel Margin="14,14,14,14">
						<TextBlock Text="{x:Static p:Resources.Replay_Recording_Settings}" FontSize="18" FontWeight="Bold"
						           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
						<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="12" LineHeight="19" Margin="0,0,0,16">
							<Run Text="{x:Static p:Resources.replays_description_part_1}"/>
								<LineBreak /><LineBreak />
							<Run Text="{x:Static p:Resources.replays_description_part_2}"/>
								<Hyperlink NavigateUri="https://echopedia.gg/wiki/Replay_Viewer"
										   RequestNavigate="Hyperlink_RequestNavigate">
									<Run Text="{x:Static p:Resources.Echo_Arena_Replay_Viewer}"/>
								</Hyperlink>
								<LineBreak /><LineBreak />
							<Run Text="{x:Static p:Resources.replays_description_part_3}"/>
								<Hyperlink NavigateUri="https://echopedia.gg/wiki/.echoreplay_File_Format"
										   RequestNavigate="Hyperlink_RequestNavigate">
									<Run Text="{x:Static p:Resources.replays_description_part_4}"/>
								</Hyperlink>
						</TextBlock>

						<CheckBox x:Name="enableFullLoggingCheckbox" Content="{x:Static p:Resources.Save_Replays}"
						          Style="{StaticResource ThemedCheckBox}" FontWeight="SemiBold" FontSize="13.5" Margin="0,0,0,10"
						          IsChecked="{l:SettingBinding enableFullLogging}" />

						<TextBlock Text="REPLAY LOGGING" Style="{StaticResource CardHeading}"/>
						<Border x:Name="fullLoggingBox" Style="{StaticResource SettingsCard}"
						        IsEnabled="{Binding ElementName=enableFullLoggingCheckbox, Path=IsChecked}">
							<StackPanel>
								<TextBlock Text="File formats" Foreground="{DynamicResource TextFaint}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,6"/>
								<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
									<CheckBox Content="Save to .echoreplay format" Style="{StaticResource ThemedCheckBox}" Margin="0,0,20,9" IsChecked="{l:SettingBinding saveEchoreplayFiles}" />
									<CheckBox Content="Save to .butter format" Style="{StaticResource ThemedCheckBox}" Margin="0,0,20,9" IsChecked="{l:SettingBinding saveButterFiles}" />
									<CheckBox Content="Save to .tape format" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,9" IsChecked="{l:SettingBinding saveTapeFiles}" />
								</StackPanel>
								<CheckBox Content="Save player bones data" Style="{StaticResource ThemedCheckBox}" IsChecked="{l:SettingBinding fetchBones}" />

								<Border Height="1" Background="{DynamicResource SurfaceBorderSoft}" Margin="0,4,0,12"/>

								<TextBlock Text="Behavior" Foreground="{DynamicResource TextFaint}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,6"/>
								<CheckBox x:Name="onlyRecordPrivateMatchesCheckBox"
											  Content="{x:Static p:Resources.Only_Record_Private_Matches}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding onlyRecordPrivateMatches}" />
								<CheckBox x:Name="batchWritesButton" Content="{x:Static p:Resources.Batch_Writes}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding batchWrites}" />
								<CheckBox x:Name="useCompressionButton" Content="{x:Static p:Resources.Use_Compression}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{l:SettingBinding useCompression}" />
								<Label Content="Recording rate:" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<ComboBox x:Name="speedSelector" Style="{StaticResource ThemedComboBox}"
											  SelectedIndex="{l:SettingBinding targetDeltaTimeIndexFull}"
											  Width="211" Margin="0,0,0,12" HorizontalAlignment="Left">
									<ComboBoxItem Content="{x:Static p:Resources.Fast__60_Hz_}" />
									<ComboBoxItem Content="{x:Static p:Resources.Med__30_Hz_}" />
									<ComboBoxItem Content="{x:Static p:Resources.Slow__10_Hz_}" />
								</ComboBox>

								<Border Height="1" Background="{DynamicResource SurfaceBorderSoft}" Margin="0,0,0,12"/>

								<Label x:Name="saveLocLabel" Content="{x:Static p:Resources.Save_Folder_}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
									<TextBox x:Name="storageLocationTextBox" TextWrapping="NoWrap" Width="240"
												 Style="{StaticResource ThemedTextBox}" Margin="0,0,8,0" MaxLines="1"
												 IsReadOnly="True"
												 Text="{l:SettingLoad saveFolder}"/>
									<Button x:Name="saveLocButton" Content="{x:Static p:Resources.Browse}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"
												Padding="10,6" Click="SetStorageLocation" />
									<Button x:Name="resetReplayFolder" Content="{x:Static p:Resources.Reset}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"
												Padding="10,6" Click="ResetReplayFolder" />
									<Button x:Name="openReplayFolderButton" Content="{x:Static p:Resources.Open_Folder}" Style="{StaticResource SecondaryButtonStyle}"
												Padding="10,6" Click="OpenReplayFolder" />
								</StackPanel>

								<Label x:Name="splitLabel" Content="{x:Static p:Resources.Replay_File_Splitting_}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
								<StackPanel Orientation="Horizontal">
									<ComboBox Visibility="Collapsed" Width="210"
												  SelectedIndex="{l:SettingBinding whenToSplitReplays}"
												  Margin="0,0,8,0">
										<ComboBoxItem Content="{x:Static p:Resources.Every_Round}" />
										<ComboBoxItem Content="{x:Static p:Resources.Every_Match__Scoreboard_}" />
										<ComboBoxItem Content="{x:Static p:Resources.On_Server_Change}" />
										<ComboBoxItem Content="{x:Static p:Resources.Only_on_IgniteBot_Restart}" />
									</ComboBox>
									<Button x:Name="splitFileButton" Content="{x:Static p:Resources.Split_File}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0"
												Padding="10,6" Click="SplitFileButtonClicked" />
									<Label x:Name="currentFilenameLabel" Content="{Binding ReplayFilename, Mode=OneWay}"
									       Foreground="{DynamicResource TextFaint}" FontFamily="Consolas" FontSize="11.5"
									       VerticalAlignment="Center"
											   ScrollViewer.VerticalScrollBarVisibility="Disabled" />
								</StackPanel>
							</StackPanel>
						</Border>
					</StackPanel>
				</ScrollViewer>
			</TabItem>


			<TabItem Header="{x:Static p:Resources.Text_to_Speech}" Padding="8,6,8,6">
				<ScrollViewer VerticalScrollBarVisibility="Auto" >
					<StackPanel Margin="14,14,14,14">
						<TextBlock Text="{x:Static p:Resources.Text_to_Speech__TTS__Settings}" FontSize="18" FontWeight="Bold"
						           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

						<Border x:Name="discordLoginWarning" Margin="0,0,0,14" Padding="12,10"
						        Background="{DynamicResource StatusWarnTint}" BorderBrush="{DynamicResource StatusWarnEdge}"
						        BorderThickness="1" CornerRadius="8"
						        Visibility="{Binding DiscordNotLoggedInVisible}">
							<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource StatusWarn}" FontSize="12">
								<Run Text="{x:Static p:Resources.TTS_works_whenever_i_want_it_too_}" />
							</TextBlock>
						</Border>

						<TextBlock x:Name="instructions" TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="12"
						           Margin="0,0,0,16" Text="{x:Static p:Resources.tts_description}" />

						<TextBlock Text="VOICE" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<Grid Margin="0,0,0,12">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="*"/>
										<ColumnDefinition Width="*"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>

									<StackPanel Grid.Column="0" Margin="0,0,10,0">
										<Label x:Name="label1" Content="{x:Static p:Resources.Speech_Speed}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
										<ComboBox HorizontalAlignment="Stretch" Style="{StaticResource ThemedComboBox}"
												  SelectedIndex="{Binding SpeechSpeed}">
											<ComboBoxItem Content="{x:Static p:Resources.Slow}" />
											<ComboBoxItem Content="{x:Static p:Resources.Normal}" />
											<ComboBoxItem Content="{x:Static p:Resources.Fast}" />
											<ComboBoxItem Content="{x:Static p:Resources.Very_Fast}" />
										</ComboBox>
									</StackPanel>

									<StackPanel Grid.Column="1" Margin="0,0,10,0">
										<Label Content="{x:Static p:Resources.Voice}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
										<ComboBox HorizontalAlignment="Stretch" Style="{StaticResource ThemedComboBox}"
												  SelectedIndex="{l:SettingBinding ttsVoice}">
											<ComboBoxItem Content="{x:Static p:Resources.Male}" />
											<ComboBoxItem Content="{x:Static p:Resources.Female}" />
										</ComboBox>
									</StackPanel>

									<CheckBox Grid.Column="2" Content="Use Wavenet Voices" Style="{StaticResource ThemedCheckBox}"
									          VerticalAlignment="Bottom" Margin="0,0,0,8" IsChecked="{l:SettingBinding useWavenetVoices}" />
								</Grid>
								<StackPanel Orientation="Horizontal">
									<Button Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" Margin="0,0,8,0" Click="ClearTTSCacheButton" Content="{x:Static p:Resources.Clear_TTS_Cache}"/>
									<Button Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" Margin="0,0,8,0" Click="OpenTTSCacheButton" Content="{x:Static p:Resources.Open_TTS_Cache_Folder}"/>
									<Button Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" Click="PlayTestSound" Content="{x:Static p:Resources.Play_Test_Sound}"/>
								</StackPanel>
							</StackPanel>
						</Border>

						<TextBlock Text="ANNOUNCEMENTS" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<CheckBox Content="{x:Static p:Resources.Server_Location}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding ServerLocation}" />
								<TextBlock Text="{x:Static p:Resources.Estimated_location_of_the_server_as_you_join}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox Content="{x:Static p:Resources.Throw_Speed}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding ThrowSpeed}" />
								<TextBlock Text="{x:Static p:Resources.Total_throw_speed_from_the_F10_menu}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="joustTimeCheckbox" Content="{x:Static p:Resources.Joust_Time}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding JoustTime}" />
								<TextBlock Text="{x:Static p:Resources.Time_in_seconds_to_reach_the_midline_on_neutral_jousts_for_the_fastest_player_on_each_team}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="joustSpeedCheckbox" Content="{x:Static p:Resources.Joust_Speed}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding JoustSpeed}" />
								<TextBlock Text="{x:Static p:Resources.Speed_in_m_s_of_the_first_player_on_each_team_to_cross_the_midline_on_neutral_joust}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="tubeExitSpeedCheckbox" Content="{x:Static p:Resources.Tube_Exit_Speed__self_}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding TubeExitSpeed}" />
								<TextBlock Text="{x:Static p:Resources.Speed_in_m_s_as_you_exit_the_launch_tube}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="maxBoostSpeedCheckbox" Content="{x:Static p:Resources.Max_Boost_Speed}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding MaxBoostSpeed}" />
								<TextBlock Text="{x:Static p:Resources.Maximum_speed_achieved_for_times_when_you_are_faster_than_20_m_s}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="goalSpeed" Content="{x:Static p:Resources.Goal_Speed}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding GoalSpeed}" />
								<TextBlock Text="{x:Static p:Resources.Speed_in_m_s_of_every_goal}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<CheckBox x:Name="goalDistance" Content="{x:Static p:Resources.Goal_Distance}" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,1"
											  IsChecked="{Binding GoalDistance}" />
								<TextBlock Text="{x:Static p:Resources.Distance_in_m_of_every_goal}"
								           Foreground="{DynamicResource TextFaint}" FontSize="11" TextWrapping="Wrap" Margin="24,0,0,9" />

								<Border Height="1" Background="{DynamicResource SurfaceBorderSoft}" Margin="0,3,0,12"/>

								<CheckBox x:Name="gamePausedCheckbox" Content="{x:Static p:Resources.Game_Paused}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{Binding GamePaused}" />
								<CheckBox x:Name="playerJoinCheckbox" Content="{x:Static p:Resources.Player_Join}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{Binding PlayerJoin}" />
								<CheckBox x:Name="playerLeaveCheckbox" Content="{x:Static p:Resources.Player_Leave}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{Binding PlayerLeave}" />
								<CheckBox x:Name="playerSwitchCheckbox" Content="{x:Static p:Resources.Player_Switch_Teams}" Style="{StaticResource ThemedCheckBox}"
											  IsChecked="{Binding PlayerSwitch}" />
								<CheckBox Content="Private Match Rules Changed" Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{Binding RulesChanged}" />
								<CheckBox Content="Ping Spike" Style="{StaticResource ThemedCheckBox}"
								          IsChecked="{Binding PingSpike}" />
								<CheckBox x:Name="ttsSpecificNumbersCheckbox" Content="Use Specific Numbers (e.g. 18.67)" Style="{StaticResource ThemedCheckBox}" Margin="0"
											  IsChecked="{Binding TTSSpecificNumbers}" />
							</StackPanel>
						</Border>
					</StackPanel>
				</ScrollViewer>
			</TabItem>



			<TabItem Header="{x:Static p:Resources.Clips}" Padding="8,6,8,6">
				<l:ClipsSettings></l:ClipsSettings>
			</TabItem>

			<TabItem Header="Media Controller" Padding="8,6,8,6">
				<l:MediaControllerSettings/>
			</TabItem>

			<!--<TabItem Header="{x:Static p:Resources.NVIDIAHighlights}" Padding="8,6,8,6">
				<ScrollViewer VerticalScrollBarVisibility="Auto">
					
				</ScrollViewer>
			</TabItem>-->

			<!--<TabItem Header="{x:Static p:Resources.Overlays}" Visibility="Visible">
				<l:OverlaysConfigWindow x:Name="OverlaysConfigWindow"/>
			</TabItem>-->


			<TabItem Header="{x:Static p:Resources.EchoVR_Settings}" Visibility="Visible">

				<ScrollViewer VerticalScrollBarVisibility="Auto">
					<StackPanel Margin="14,14,14,14">
						<TextBlock Text="{x:Static p:Resources.EchoVR_Settings}" FontSize="18" FontWeight="Bold"
						           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

						<Border Margin="0,0,0,14" Padding="12,10"
						        Background="{DynamicResource StatusWarnTint}" BorderBrush="{DynamicResource StatusWarnEdge}"
						        BorderThickness="1" CornerRadius="8"
						        Visibility="{Binding EchoVRNotInstalled, Mode=OneWay}">
							<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource StatusWarn}" FontSize="12"
							           Text="{x:Static p:Resources.EchoVR_not_installed_game_settings}" />
						</Border>

						<StackPanel Visibility="{Binding EchoVRInstalled}">
							<Border Margin="0,0,0,14" Padding="12,10"
							        Background="{DynamicResource StatusWarnTint}" BorderBrush="{DynamicResource StatusWarnEdge}"
							        BorderThickness="1" CornerRadius="8"
							        Visibility="{Binding EchoVRSettingsProgramOpenWarning, Mode=OneWay}">
								<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource StatusWarn}" FontSize="12"
								           Text="{x:Static p:Resources.EchoVROpen_settings_warning}" />
							</Border>

							<StackPanel>
								<StackPanel Orientation="Horizontal" Margin="0,0,0,14">
									<CheckBox Content="{x:Static p:Resources.Enable_API_Access}" Style="{StaticResource ThemedCheckBox}" Margin="0"
									          IsChecked="{Binding Source={StaticResource es}, Path=EnableAPIAccess}"/>
									<Button Content="{x:Static p:Resources.Reset_All_Settings_to_Default}" Style="{StaticResource SecondaryButtonStyle}"
									        Click="ResetAllSettings" HorizontalAlignment="Right" Margin="30,0,0,0" Padding="10,6"/>
								</StackPanel>

								<TextBlock Text="SETTINGS TARGET" Style="{StaticResource CardHeading}"/>
								<Border Style="{StaticResource SettingsCard}" ToolTip="{x:Static p:Resources.EchoVR_Settings_info}">
									<StackPanel>
										<StackPanel Orientation="Horizontal" Margin="0,0,0,16">
											<Label Content="{x:Static p:Resources.Apply_these_settings_to_}" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
											<ComboBox SelectedIndex="{Binding Source={StaticResource es}, Path=CaptureVP2}" Style="{StaticResource ThemedComboBox}"
											          Width="180" SelectionChanged="RefreshAllSettings">
												<ComboBoxItem Content="{x:Static p:Resources.Default_Configuration}"/>
												<ComboBoxItem Content="capturevp2"/>
											</ComboBox>
										</StackPanel>

										<Expander Header="{x:Static p:Resources.Display}" Style="{StaticResource CardExpander}">
											<StackPanel Margin="0,12,0,0">
												<CheckBox x:Name="fullscreenCheckbox" Content="{x:Static p:Resources.Fullscreen}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding Fullscreen}"/>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Monitor}" Style="{StaticResource SliderRowLabel}"/>
													<TextBox x:Name="monitorField" Text="{Binding Monitor}" Style="{StaticResource SliderValueBox}"/>
												</StackPanel>
												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Resolution}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="resolutionSlider" Value="{Binding Resolution}" Minimum="0.1" Maximum="2" Width="200" Height="20" SmallChange="0.1" TickFrequency="0.1" LargeChange="0.5" TickPlacement="None"/>
													<TextBox Text="{Binding ElementName=resolutionSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
												</StackPanel>
												<CheckBox Content="{x:Static p:Resources.Multi_Resolution_Shading}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding MultiResShading}"/>
												<CheckBox Content="{x:Static p:Resources.Auto_Resolution}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding Source={StaticResource es}, Path=AutoRes}"/>
												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Field_of_View}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="mainFovSlider" Value="{Binding FoV}" Minimum="40" Maximum="140" Width="200" Height="20"/>
													<TextBox Text="{Binding ElementName=mainFovSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.Field_of_View_only_works_in__capturevp2_mode}" Style="{StaticResource HintText}" Margin="0,4,0,0"/>
											</StackPanel>
										</Expander>

										<Expander Header="{x:Static p:Resources.Graphics}" Style="{StaticResource CardExpander}">
											<StackPanel Margin="0,12,0,0">
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Anti_aliasing}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding AA}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.None}"/>
														<ComboBoxItem Content="2x"/>
														<ComboBoxItem Content="4x"/>
													</ComboBox>
												</StackPanel>
												<CheckBox Content="{x:Static p:Resources.Temporal_AA}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding TemporalAA}"/>
												<CheckBox Content="{x:Static p:Resources.Volumetrics}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding Volumetrics}"/>
												<CheckBox Content="{x:Static p:Resources.Bloom}" Style="{StaticResource ThemedCheckBox}" IsChecked="{Binding Bloom}"/>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Shadow_Quality}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding ShadowQuality}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.Low}"/>
														<ComboBoxItem Content="{x:Static p:Resources.Medium}"/>
														<ComboBoxItem Content="{x:Static p:Resources.High}"/>
													</ComboBox>
												</StackPanel>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Mesh_Quality}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding MeshQuality}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.Low}"/>
														<ComboBoxItem Content="{x:Static p:Resources.Medium}"/>
														<ComboBoxItem Content="{x:Static p:Resources.High}"/>
													</ComboBox>
												</StackPanel>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.FX_Quality}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding FXQuality}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.Low}"/>
														<ComboBoxItem Content="{x:Static p:Resources.Medium}"/>
														<ComboBoxItem Content="{x:Static p:Resources.High}"/>
													</ComboBox>
												</StackPanel>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Texture_Quality}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding TextureQuality}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.Low}"/>
														<ComboBoxItem Content="{x:Static p:Resources.Medium}"/>
														<ComboBoxItem Content="{x:Static p:Resources.High}"/>
													</ComboBox>
												</StackPanel>
												<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
													<Label Content="{x:Static p:Resources.Lighting_Quality}" Style="{StaticResource SliderRowLabel}"/>
													<ComboBox SelectedIndex="{Binding LightingQuality}" Style="{StaticResource ThemedComboBox}" Width="140">
														<ComboBoxItem Content="{x:Static p:Resources.Low}"/>
														<ComboBoxItem Content="{x:Static p:Resources.Medium}"/>
														<ComboBoxItem Content="{x:Static p:Resources.High}"/>
													</ComboBox>
												</StackPanel>
												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Sharpening}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="sharpeningSlider" Value="{Binding Sharpening}" Minimum="0" Maximum="2" Width="200" Height="20" LargeChange="0.5" SmallChange="0.1" TickFrequency="0.1" TickPlacement="None"/>
													<TextBox Text="{Binding ElementName=sharpeningSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
												</StackPanel>
											</StackPanel>
										</Expander>
									</StackPanel>
								</Border>

								<TextBlock Text="SOUND" Style="{StaticResource CardHeading}"/>
								<Expander Header="{x:Static p:Resources.Sound_Settings}" Style="{StaticResource CardExpander}">
									<StackPanel Margin="0,12,0,0">
										<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
											<Label Content="{x:Static p:Resources.Announcer_Volume}" Style="{StaticResource SliderRowLabel}"/>
											<Slider x:Name="announcerVolumeSlider" Value="{Binding Source={StaticResource es}, Path=AnnouncerVolume}" Minimum="0" Maximum="100" Width="200" Height="20" TickFrequency="10" SmallChange="10"/>
											<TextBox Text="{Binding ElementName=announcerVolumeSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
											<Label Content="%" Style="{StaticResource UnitLabel}"/>
										</StackPanel>
										<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
											<Label Content="{x:Static p:Resources.Music_Volume}" Style="{StaticResource SliderRowLabel}"/>
											<Slider x:Name="musicVolumeSlider" Value="{Binding Source={StaticResource es}, Path=MusicVolume}" Minimum="0" Maximum="100" Width="200" Height="20" TickFrequency="10" SmallChange="10"/>
											<TextBox Text="{Binding ElementName=musicVolumeSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
											<Label Content="%" Style="{StaticResource UnitLabel}"/>
										</StackPanel>
										<StackPanel Orientation="Horizontal" Margin="0,0,0,9">
											<Label Content="{x:Static p:Resources.Sound_Effects_Volume}" Style="{StaticResource SliderRowLabel}"/>
											<Slider x:Name="sfxVolumeSlider" Value="{Binding Source={StaticResource es}, Path=SFXVolume}" Minimum="0" Maximum="100" Width="200" Height="20" TickFrequency="10" SmallChange="10"/>
											<TextBox Text="{Binding ElementName=sfxVolumeSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
											<Label Content="%" Style="{StaticResource UnitLabel}"/>
										</StackPanel>
										<StackPanel Orientation="Horizontal">
											<Label Content="{x:Static p:Resources.VOIP_Volume}" Style="{StaticResource SliderRowLabel}"/>
											<Slider x:Name="voipVolumeSlider" Value="{Binding Source={StaticResource es}, Path=VOIPVolume}" Minimum="0" Maximum="100" Width="200" Height="20" TickFrequency="10" SmallChange="10"/>
											<TextBox Text="{Binding ElementName=voipVolumeSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
											<Label Content="%" Style="{StaticResource UnitLabel}"/>
										</StackPanel>
									</StackPanel>
								</Expander>

								<TextBlock Text="SPECTATOR CAMERA" Style="{StaticResource CardHeading}"/>
								<Expander Header="{x:Static p:Resources.Spectator_Camera_Settings}" Style="{StaticResource CardExpander}">
									<StackPanel Margin="0,12,0,0">
										<TextBlock Text="{x:Static p:Resources.Free_Cam_Settings___C_on_the_keyboard_}" FontSize="12" FontWeight="SemiBold"
										           Foreground="{DynamicResource TextDim}" Margin="0,0,0,8"
										           ToolTip="{x:Static p:Resources.These_settings_control_game_settings_found_in__C__Program_Files_Oculus_Software_Software_ready_at_dawn_echo_arena_sourcedb_rad15_json_r14_config_mp_spectator_settings_json___This_is_useful_for_spectator_computers_without_a_headset_attached_}"/>
										<Border Style="{StaticResource InnerCard}">
											<StackPanel>
												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Free_Cam_Field_of_View}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="freeCamFovSlider" Value="{Binding Source={StaticResource es}, Path=FreeCamFov}" Minimum="20" Maximum="140" Width="180" Height="20"/>
													<TextBox Text="{Binding ElementName=freeCamFovSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="{x:Static p:Resources.deg}" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.FOV_for_free_camera}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="Movement Speed" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="moveSpeedSlider" Value="{Binding Source={StaticResource es}, Path=FreeCamMaxSpeed}" Minimum="0" Maximum="16" Width="180" Height="20" SmallChange="0.1" TickFrequency="0.1"/>
													<TextBox Text="{Binding ElementName=moveSpeedSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="m/s" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.Maximum_translational_movement_speed}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Vertical_Move_Speed}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="moveSpeedVerticalSlider" Value="{Binding Source={StaticResource es}, Path=FreeCamMaxSpeedVertical}" Minimum="0" Maximum="16" Width="180" Height="20" TickFrequency="0.1"/>
													<TextBox Text="{Binding ElementName=moveSpeedVerticalSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="m/s" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.Maximum_translational_movement_speed_on_the_vertical_axis}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Rotation_Speed}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="freeCamRotationSpeedSlider" Value="{Binding Source={StaticResource es}, Path=FreeCamRotationSpeed}" Minimum="0" Maximum="10" Width="180" Height="20" TickFrequency="0.1"/>
													<TextBox Text="{Binding ElementName=freeCamRotationSpeedSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.Maximum_rotation_speed_of_the_camera}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Speed_Multiplier}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="freeCamSpeedMultSlider" Value="{Binding Source={StaticResource es}, Path=FreeCamSpeedMult}" Minimum="0" Maximum="5" Width="180" Height="20" SmallChange="0.1" TickFrequency="0.1"/>
													<TextBox Text="{Binding ElementName=freeCamSpeedMultSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="x" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.Speed_multiplier_for_movement_speed_when_pressing_the_speed_modifier_button}" Style="{StaticResource HintText}" Margin="0,0,0,0"/>
											</StackPanel>
										</Border>

										<TextBlock Text="For the 2D spectator" FontSize="12" FontWeight="SemiBold"
										           Foreground="{DynamicResource TextDim}" Margin="0,4,0,8"
										           ToolTip="{x:Static p:Resources.These_settings_control_game_settings_found_in__C__Program_Files_Oculus_Software_Software_ready_at_dawn_echo_arena_sourcedb_rad15_json_r14_config_mp_spectator_settings_json___This_is_useful_for_spectator_computers_without_a_headset_attached_}"/>
										<Border Style="{StaticResource InnerCard}" Margin="0">
											<StackPanel>
												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Spectator_Field_of_View}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="specFovSlider" Value="{Binding Source={StaticResource es}, Path=SpecFovSlider}" Minimum="40" Maximum="140" Width="180" Height="20"/>
													<TextBox Text="{Binding ElementName=specFovSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="{x:Static p:Resources.deg}" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.FOV_for_auto_cam}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.POV_Field_of_View}" Style="{StaticResource SliderRowLabel}"/>
													<Slider x:Name="povFovSlider" Value="{Binding Source={StaticResource es}, Path=POVFov}" Minimum="40" Maximum="140" Width="180" Height="20"/>
													<TextBox Text="{Binding ElementName=povFovSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SliderValueBox}"/>
													<Label Content="{x:Static p:Resources.deg}" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
												<TextBlock Text="{x:Static p:Resources.FOV_for_POV_cam___P_on_the_keyboard_when_following_a_player_}" Style="{StaticResource HintText}"/>

												<StackPanel Orientation="Horizontal">
													<Label Content="{x:Static p:Resources.Follow_Cam_Offset}" Style="{StaticResource SliderRowLabel}"/>
													<TextBox Text="{Binding Source={StaticResource es}, Path=FollowCamOffsetX}" Style="{StaticResource SliderValueBox}" Margin="0,0,4,0"/>
													<TextBox Text="{Binding Source={StaticResource es}, Path=FollowCamOffsetY}" Style="{StaticResource SliderValueBox}" Margin="0,0,4,0"/>
													<TextBox Text="{Binding Source={StaticResource es}, Path=FollowCamOffsetZ}" Style="{StaticResource SliderValueBox}" Margin="0,0,4,0"/>
													<Label Content="x,y,z" Style="{StaticResource UnitLabel}"/>
												</StackPanel>
											</StackPanel>
										</Border>
									</StackPanel>
								</Expander>
							</StackPanel>
						</StackPanel>
					</StackPanel>
				</ScrollViewer>
			</TabItem>


			<TabItem Header="{x:Static p:Resources.About}" Padding="8,6,8,6">
				<ScrollViewer VerticalScrollBarVisibility="Auto">
					<StackPanel Margin="14,14,14,14">
						<TextBlock Text="{x:Static p:Resources.About}" FontSize="18" FontWeight="Bold"
						           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

						<Border Style="{StaticResource SettingsCard}">
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="150"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<StackPanel>
									<Image x:Name="logo" Source="/img/ignite_logo_128.png" Width="112"
									   Height="112" HorizontalAlignment="Center" VerticalAlignment="Top" />
									<Label x:Name="versionNum" Content="{Binding AppVersionLabelText}" Width="128" Margin="0,8,0,0"
									   Foreground="{DynamicResource TextDim}" FontFamily="Consolas" FontSize="11.5"
									   HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
									   ScrollViewer.VerticalScrollBarVisibility="Disabled" Padding="0" Height="24"
									   HorizontalAlignment="Center" VerticalAlignment="Top" />
								</StackPanel>
								<StackPanel Grid.Column="1" VerticalAlignment="Center">
									<TextBlock TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource TextPrimary}" FontSize="12.5" LineHeight="19">
									<Run Text="{x:Static p:Resources.IgniteBot_was_made_from_contributions_by_these_community_members_}" Foreground="{DynamicResource TextFaint}" />

									<LineBreak />
									NtsFranz
									<LineBreak />
									VTSxKING
									<LineBreak />
									iblowatsports
									<LineBreak />
									Graic
									<LineBreak />
									EnvyEm
									<LineBreak />
									h4ck3r210
									<LineBreak/>
									he_is_the_cat
									<LineBreak/>
									<LineBreak/>
									<Run Text="{x:Static p:Resources.Japanese_Translation_}" Foreground="{DynamicResource TextFaint}" />
									<LineBreak/>
									ルーク/ lluukkeeVR
									<LineBreak/>
									iKaika_Higashi
									<LineBreak/>
									Yuuking
									<LineBreak/>
									<LineBreak/>
									<Run Text="Artists" Foreground="{DynamicResource TextFaint}" />
									<LineBreak/>
									Sickmave
									<LineBreak/>
									</TextBlock>
								</StackPanel>
							</Grid>
						</Border>

						<TextBlock Text="LINKS" Style="{StaticResource CardHeading}"/>
						<Border Style="{StaticResource SettingsCard}">
							<StackPanel>
								<TextBlock Text="{x:Static p:Resources.Contribute}" Foreground="{DynamicResource TextFaint}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
								<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="12.5" Margin="0,0,0,14">
									<Run Text="{x:Static p:Resources.contributions_welcome}"/>
										<Hyperlink NavigateUri="https://github.com/heisthecat31/Spark" RequestNavigate="Hyperlink_RequestNavigate">
											GitHub
										</Hyperlink>
								</TextBlock>

								<TextBlock Text="{x:Static p:Resources.Help}" Foreground="{DynamicResource TextFaint}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
								<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="12.5" Margin="0,0,0,14">
										<Run Text="{x:Static p:Resources.for_help_check_out}"/>
										<Hyperlink NavigateUri="https://echopedia.gg/wiki/Spark"
												   RequestNavigate="Hyperlink_RequestNavigate">
											<Run Text="{x:Static p:Resources.Echopedia_page}"/>
										</Hyperlink>
									<Run Text="{x:Static p:Resources.__or_join_the}"/>
										<Hyperlink NavigateUri="https://discord.gg/CGXR86C"
												   RequestNavigate="Hyperlink_RequestNavigate">
											<Run Text="{x:Static p:Resources.Ignite_Discord_Server}"/>
										</Hyperlink>
										<Run Text="{x:Static p:Resources.and_ask_questions_in_the__ignitebot_support_channel_}"/>
								</TextBlock>

								<TextBlock Text="{x:Static p:Resources.Donate}" Foreground="{DynamicResource TextFaint}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
								<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="12.5" Margin="0">
									<Run Text="{x:Static p:Resources.Donations_are_not_expected_but_are_appreciated_}"/>
									<Hyperlink NavigateUri="https://ignitevr.gg/spark"
									           RequestNavigate="Hyperlink_RequestNavigate" >
										<Run Text="{x:Static p:Resources.Donate}"/>
									</Hyperlink>
								</TextBlock>
							</StackPanel>
						</Border>
					</StackPanel>
				</ScrollViewer>
			</TabItem>

			<!-- ═══════════════════════════════════════════════════════════════════════════
			     CHANGE THEME TAB
			     ═══════════════════════════════════════════════════════════════════════════ -->
			<TabItem Header="Change Theme" Padding="8,6,8,6"
			         x:Name="ChangeThemeTab"
			         Loaded="ChangeThemeTab_Loaded">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<StackPanel Margin="14,14,14,14">

					<TextBlock Text="Change Theme" FontSize="18" FontWeight="Bold"
					           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,4"/>
					<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" Margin="0,0,0,16" FontSize="12"
					           Text="Pick three colours to recolour the entire app live. Changes apply immediately and persist between restarts."/>

					<!-- Dark Colour -->
					<Border Style="{StaticResource SettingsCard}">
						<StackPanel>
							<TextBlock Text="Dark (Backgrounds)" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
							<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
								<Rectangle x:Name="DarkPreviewRect" Width="36" Height="36" RadiusX="6" RadiusY="6"
								           Margin="0,0,10,0" VerticalAlignment="Center"/>
								<TextBox x:Name="DarkHexBox" Width="90" Style="{StaticResource ThemedTextBox}"
								         FontFamily="Consolas" FontSize="13"
								         TextChanged="DarkHexBox_TextChanged" MaxLength="7"/>
								<TextBlock Text="Hex" VerticalAlignment="Center" Foreground="{DynamicResource TextFaint}" FontSize="11.5" Margin="8,0,0,0"/>
							</StackPanel>
							<Label Content="Hue" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="DarkHueSlider" Minimum="0" Maximum="360" SmallChange="1" LargeChange="10"
							        ValueChanged="DarkSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Saturation" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="DarkSatSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="DarkSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Brightness" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="DarkValSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="DarkSlider_Changed" IsSnapToTickEnabled="False"/>
						</StackPanel>
					</Border>

					<!-- Mid Colour -->
					<Border Style="{StaticResource SettingsCard}">
						<StackPanel>
							<TextBlock Text="Mid (Buttons &amp; Borders)" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
							<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
								<Rectangle x:Name="MidPreviewRect" Width="36" Height="36" RadiusX="6" RadiusY="6"
								           Margin="0,0,10,0" VerticalAlignment="Center"/>
								<TextBox x:Name="MidHexBox" Width="90" Style="{StaticResource ThemedTextBox}"
								         FontFamily="Consolas" FontSize="13"
								         TextChanged="MidHexBox_TextChanged" MaxLength="7"/>
								<TextBlock Text="Hex" VerticalAlignment="Center" Foreground="{DynamicResource TextFaint}" FontSize="11.5" Margin="8,0,0,0"/>
							</StackPanel>
							<Label Content="Hue" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="MidHueSlider" Minimum="0" Maximum="360" SmallChange="1" LargeChange="10"
							        ValueChanged="MidSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Saturation" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="MidSatSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="MidSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Brightness" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="MidValSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="MidSlider_Changed" IsSnapToTickEnabled="False"/>
						</StackPanel>
					</Border>

					<!-- Light Colour -->
					<Border Style="{StaticResource SettingsCard}">
						<StackPanel>
							<TextBlock Text="Light (Hover &amp; Highlights)" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
							<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
								<Rectangle x:Name="LightPreviewRect" Width="36" Height="36" RadiusX="6" RadiusY="6"
								           Margin="0,0,10,0" VerticalAlignment="Center"/>
								<TextBox x:Name="LightHexBox" Width="90" Style="{StaticResource ThemedTextBox}"
								         FontFamily="Consolas" FontSize="13"
								         TextChanged="LightHexBox_TextChanged" MaxLength="7"/>
								<TextBlock Text="Hex" VerticalAlignment="Center" Foreground="{DynamicResource TextFaint}" FontSize="11.5" Margin="8,0,0,0"/>
							</StackPanel>
							<Label Content="Hue" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="LightHueSlider" Minimum="0" Maximum="360" SmallChange="1" LargeChange="10"
							        ValueChanged="LightSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Saturation" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="LightSatSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="LightSlider_Changed" Margin="0,0,0,6" IsSnapToTickEnabled="False"/>
							<Label Content="Brightness" Style="{StaticResource FieldLabel}" Margin="0,0,0,2"/>
							<Slider x:Name="LightValSlider" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"
							        ValueChanged="LightSlider_Changed" IsSnapToTickEnabled="False"/>
						</StackPanel>
					</Border>

					<TextBlock Text="PRESETS" Style="{StaticResource CardHeading}"/>
					<Border Style="{StaticResource SettingsCard}">
						<WrapPanel>
							<Button x:Name="PresetBlackBtn"      Content="🐈 Black"      Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetBlack_Click"/>
							<Button x:Name="PresetBlueBtn"       Content="🌌 Blue"       Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetBlue_Click"/>
							<Button x:Name="PresetCoolPurpleBtn" Content="🔮 Cool Purple" Style="{StaticResource SecondaryButtonStyle}" Width="110" Margin="0,0,8,8" Click="PresetCoolPurple_Click"/>
							<Button x:Name="PresetDarkRedBtn"    Content="🔥 Dark Red"    Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetDarkRed_Click"/>
							<Button x:Name="PresetGreenBtn"      Content="🌳 Green"      Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetGreen_Click"/>
							<Button x:Name="PresetHotPinkBtn"    Content="💖 Hot Pink"    Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetHotPink_Click"/>
							<Button x:Name="PresetPurpleBtn"     Content="🟣 Purple"      Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetPurple_Click"/>
							<Button x:Name="PresetRedBtn"        Content="🔴 Red"         Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetRed_Click"/>
							<Button x:Name="PresetYellowBtn"     Content="🌞 Yellow"      Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,8,8" Click="PresetYellow_Click"/>
							<Button x:Name="PresetPinkBtn"       Content="🌸 Spark Pink"  Style="{StaticResource SecondaryButtonStyle}" Width="105" Margin="0,0,8,8" Click="PresetPink_Click"/>
							<Button x:Name="PresetPVUBtn"        Content="🔵 PVU Blue"    Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,0,8" Click="PresetPVU_Click"/>
						</WrapPanel>
					</Border>

					<!-- Apply / Reset Row -->
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
						<Button x:Name="ResetThemeBtn" Content="Reset to Default" Style="{StaticResource SecondaryButtonStyle}" Width="130" Padding="10,8" Margin="0,0,10,0"
						        Click="ResetTheme_Click"/>
						<Button x:Name="ApplyThemeBtn" Content="Apply Theme" Style="{StaticResource AccentButtonStyle}" Width="130" Padding="10,8"
						        Click="ApplyTheme_Click"/>
					</StackPanel>

				</StackPanel>
			</ScrollViewer>
			</TabItem>

		</TabControl>

<Button Content="{x:Static p:Resources.Close}" Width="100" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Right"
				Margin="10,10,10,10" Click="CloseButtonEvent" Grid.Row="2" />

		<Grid x:Name="QuestSpectatorPopup" Visibility="Collapsed" Background="#CC000000" Panel.ZIndex="1000" Grid.RowSpan="3">
			<Border Background="{DynamicResource SurfaceGround}" BorderBrush="{DynamicResource SurfaceBorder}" BorderThickness="1"
					HorizontalAlignment="Center" VerticalAlignment="Center" Padding="22" CornerRadius="12" Width="450">
				<StackPanel>
					<TextBlock Text="Simple Spectate Mode" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
							   HorizontalAlignment="Center" Margin="0,0,0,6"/>
					<TextBlock Text="Spectate anyone. Follows them from match to match automatically, into private matches and lobbies too."
							   Foreground="{DynamicResource TextDim}" FontSize="11" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,16"/>

					<Label Content="Who to spectate" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
					<RadioButton x:Name="FollowFriendRadio" GroupName="QuestSpectatorTarget" Content="Anyone, by name"
								 Foreground="{DynamicResource TextPrimary}" FontSize="12.5" Margin="0,0,0,6" IsChecked="True"
								 Checked="QuestSpectatorModeChanged"
								 ToolTip="Checks your Friends list first, then falls back to searching public matches by name."/>
					<RadioButton x:Name="FollowSelfRadio" GroupName="QuestSpectatorTarget" Content="Myself, on my other PC"
								 Foreground="{DynamicResource TextPrimary}" FontSize="12.5" Margin="0,0,0,10"
								 Checked="QuestSpectatorModeChanged"
								 ToolTip="Log into Discord on both PCs. This client follows whatever session the playing PC reports."/>

					<StackPanel x:Name="QuestSpectatorNamePanel">
						<Label Content="Name" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
						<TextBox x:Name="TargetNameInput" Style="{StaticResource ThemedTextBox}" Margin="0,0,0,4"/>
						<TextBlock Text="Their Echo VR or Discord name. Anyone can be followed into public matches; add them as a friend and you can follow them into private matches and lobbies as well."
								   Foreground="{DynamicResource TextFaint}" FontSize="10.5" FontStyle="Italic" TextWrapping="Wrap" Margin="0,0,0,14"/>
					</StackPanel>

					<StackPanel x:Name="QuestSpectatorSelfPanel" Visibility="Collapsed">
						<TextBlock Text="Log into Discord in Spark on both PCs. The playing PC publishes the session it is in and this one follows it, private matches and lobbies included."
								   Foreground="{DynamicResource TextFaint}" FontSize="10.5" FontStyle="Italic" TextWrapping="Wrap" Margin="0,0,0,14"/>
					</StackPanel>

					<CheckBox x:Name="AnonymousModeCheckbox" Content="Use anonymous mode" Style="{StaticResource ThemedCheckBox}"
							  IsChecked="True"
							  ToolTip="Launches EchoVR with -noovr so this spectator client uses an anonymous account instead of your real one."/>

					<CheckBox x:Name="AutoJoinCheckbox" Content="Keep following them between sessions" Style="{StaticResource ThemedCheckBox}" Margin="0,0,0,4"
							  IsChecked="True"
							  ToolTip="If checked, Spark keeps watching where they are and moves this spectator client whenever they change session."/>

					<TextBlock x:Name="QuestSpectatorStatusText" Text="" Foreground="{DynamicResource TextDim}" FontSize="11"
							   TextWrapping="Wrap" Margin="0,10,0,0" Visibility="Collapsed"/>

					<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
						<Button x:Name="StopQuestSpectatorButton" Content="Stop Following" Style="{StaticResource SecondaryButtonStyle}"
								Click="StopQuestSpectator" Margin="0,0,10,0" Padding="16,7" Visibility="Collapsed"/>
						<Button Content="Close" Style="{StaticResource SecondaryButtonStyle}" Click="CloseSpectatorPopup" Margin="0,0,10,0" Padding="16,7"/>
						<Button x:Name="LaunchQuestSpectatorButton" Content="Launch Spectator" Style="{StaticResource AccentButtonStyle}" Click="LaunchQuestSpectator" Padding="16,7"/>
					</StackPanel>
				</StackPanel>
			</Border>
		</Grid>
	</Grid>
</Window>
