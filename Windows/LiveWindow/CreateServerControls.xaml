<UserControl x:Class="Spark.CreateServerControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:p="clr-namespace:Spark.Properties"
             xmlns:l="clr-namespace:Spark"
             xmlns:w="clr-namespace:Spark.Windows"
             xmlns:local="clr-namespace:Spark"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1400"
             Background="{DynamicResource SurfaceGround}"
             FontSize="16"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <UserControl.Resources>
        <Style x:Key="CardHeading" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFaint}"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource TextDim}"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ThemedComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="ThemedCheckBox" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="ServerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="14,11"/>
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource ControlMouseOverBorderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusBadgeOpenStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource StatusGoodTint}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource StatusGoodEdge}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="6,2"/>
        </Style>

        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextDim}"/>
            <Setter Property="FontSize" Value="10.5"/>
        </Style>

        <Style x:Key="ServerBrowserButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlMouseOverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ControlAccent}"/>
            <Setter Property="Foreground" Value="{DynamicResource ControlAccentForeground}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlBrightDefaultBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaised}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlMouseOverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource SurfaceGround}" Margin="16,14,16,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Create Server -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="CREATE SERVER" Style="{StaticResource CardHeading}" Margin="2,0,0,8"/>
                <TextBlock Text="{x:Static p:Resources.This_creates_a_private_match_in_a_region_of_your_choice_}"
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="{DynamicResource TextDim}"
                           Margin="2,0,0,14"/>

                <Border Background="{DynamicResource SurfaceCard}" BorderBrush="{DynamicResource SurfaceBorder}"
                        BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <Label Content="Region" Style="{StaticResource FieldLabel}"/>
                        <ComboBox SelectedIndex="{l:SettingBinding chooseRegionIndex}"
                                  Style="{StaticResource ThemedComboBox}"
                                  Height="54">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock FontSize="11" Foreground="{DynamicResource TextFaint}">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0}">
                                                    <Binding Path="Tag"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                            <ComboBoxItem Content="{x:Static p:Resources.US_Central_North}" Tag="Chicago, Illinois"/>
                            <ComboBoxItem Content="{x:Static p:Resources.US_Central_South}" Tag="Dallas, Texas"/>
                            <ComboBoxItem Content="US Central South 2" Tag="Houston, Texas"/>
                            <ComboBoxItem Content="{x:Static p:Resources.US_East}" Tag="{x:Static p:Resources.e_g__New_York__Miami__Charleston}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.US_West}" Tag="{x:Static p:Resources.e_g__California}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Europe}" Tag="{x:Static p:Resources.e_g__London__Amsterdam__Brussels}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Japan}" Tag="{x:Static p:Resources.e_g__Tokyo}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Australia}" Tag="{x:Static p:Resources.e_g__Sydney__Perth}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Singapore}" Tag="{x:Static p:Resources.e_g__Singapore}"/>
                        </ComboBox>

                        <Label Content="Map" Style="{StaticResource FieldLabel}"/>
                        <ComboBox SelectedIndex="{l:SettingBinding chooseMapIndex}"
                                  Style="{StaticResource ThemedComboBox}"
                                  Height="34">
                            <ComboBoxItem Content="{x:Static p:Resources.Arena}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Lobby}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Combat_Dyson}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Combat_Combustion}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Combat_Fission}"/>
                            <ComboBoxItem Content="{x:Static p:Resources.Combat_Surge}"/>
                        </ComboBox>

                        <Label Content="Game Type" Style="{StaticResource FieldLabel}"/>
                        <ComboBox SelectedIndex="{l:SettingBinding chooseGameTypeIndex}"
                                  Style="{StaticResource ThemedComboBox}"
                                  Height="34"
                                  ToolTip="Not all of these will work">
                            <ComboBoxItem Content="(default)"/>
                            <ComboBoxItem Content="Social_2.0_Private"/>
                            <ComboBoxItem Content="Social_2.0_NPE"/>
                            <ComboBoxItem Content="Social_2.0"/>
                            <ComboBoxItem Content="Echo_Arena"/>
                            <ComboBoxItem Content="Echo_Arena_Tournament"/>
                            <ComboBoxItem Content="Echo_Arena_Public_AI"/>
                            <ComboBoxItem Content="Echo_Arena_Practice_AI"/>
                            <ComboBoxItem Content="Echo_Arena_Private_AI"/>
                            <ComboBoxItem Content="Echo_Arena_First_Match"/>
                            <ComboBoxItem Content="Echo_Demo"/>
                            <ComboBoxItem Content="Echo_Demo_Public"/>
                            <ComboBoxItem Content="Echo_Arena_NPE"/>
                            <ComboBoxItem Content="Echo_Arena_Private"/>
                            <ComboBoxItem Content="Echo_Combat"/>
                            <ComboBoxItem Content="Echo_Combat_Tournament"/>
                            <ComboBoxItem Content="Echo_Combat_Private"/>
                            <ComboBoxItem Content="Echo_Combat_Public_AI"/>
                            <ComboBoxItem Content="Echo_Combat_Practice_AI"/>
                            <ComboBoxItem Content="Echo_Combat_Private_AI"/>
                            <ComboBoxItem Content="Echo_Combat_First_Match"/>
                        </ComboBox>

                        <CheckBox x:Name="spectatorModeCheckbox"
                                  Content="{x:Static p:Resources.Spectator_Mode}"
                                  ToolTip="{x:Static p:Resources.chooseRegionSpectator_ToolTip}"
                                  IsChecked="{l:SettingBinding chooseRegionSpectator}"
                                  Style="{StaticResource ThemedCheckBox}"
                                  Margin="0,6,0,6"/>

                        <CheckBox Content="{x:Static p:Resources.noovrCheckbox_Content}"
                                  ToolTip="{x:Static p:Resources.Anonymous_accounts_allow_you_to_join_the_match_twice_on_the_same_computer_}"
                                  IsChecked="{l:SettingBinding chooseRegionNoOVR}"
                                  Style="{StaticResource ThemedCheckBox}"
                                  Margin="0,0,0,16"
                                  IsEnabled="{Binding ElementName=spectatorModeCheckbox, Path=IsChecked}"/>

                        <Button Content="LAUNCH SERVER"
                                Click="Create"
                                Style="{StaticResource AccentButtonStyle}"
                                Height="46"/>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource SurfaceCard}" BorderBrush="{DynamicResource SurfaceBorder}"
                        BorderThickness="1" CornerRadius="8" Padding="14">
                    <StackPanel>
                        <Label Content="OfflineEcho" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Install"
                                    Click="InstallOfflineEcho"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="20,8"
                                    Width="120"
                                    Margin="0,0,10,0"/>
                            <Button Content="Remove"
                                    Click="RemoveOfflineEcho"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="20,8"
                                    Width="120"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Server Browser -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource SurfaceCard}" BorderBrush="{DynamicResource SurfaceBorder}"
                    BorderThickness="1" CornerRadius="8" Padding="16,14" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Server Browser"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Browse and join Echo Arena / Combat servers"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextDim}"
                                   Margin="0,3,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="StatusIndicator" Width="8" Height="8" Fill="{DynamicResource StatusBad}" VerticalAlignment="Center" Margin="0,0,7,0"/>
                        <TextBlock x:Name="StatusTextBlock"
                                   Text="Disconnected"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextDim}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,18,0"/>

                        <Button Content="Refresh"
                                Click="RefreshServers_Click"
                                Style="{StaticResource ServerBrowserButtonStyle}"
                                Margin="0,0,8,0"/>

                        <Button Content="Settings"
                                Click="ShowSettings_Click"
                                Style="{StaticResource ServerBrowserButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="360"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource SurfaceCard}"
                        CornerRadius="8"
                        Margin="0,0,10,0"
                        BorderBrush="{DynamicResource SurfaceBorder}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                CornerRadius="8,8,0,0"
                                Padding="14,10"
                                BorderBrush="{DynamicResource SurfaceBorder}"
                                BorderThickness="0,0,0,1">
                            <Grid>
                                <TextBlock Text="Available Servers"
                                           Style="{StaticResource CardHeading}"
                                           VerticalAlignment="Center"/>

                                <TextBlock x:Name="ServerCountLabel"
                                           Text="0 servers"
                                           FontSize="11"
                                           FontFamily="Consolas"
                                           Foreground="{DynamicResource TextFaint}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ServerListControl" Margin="10,10,10,10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ServerCardStyle}"
                                                MouseLeftButtonDown="ServerCard_MouseLeftButtonDown">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <Grid Grid.Row="0" Margin="0,0,0,7">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0"
                                                               Text="{Binding DisplayName}"
                                                               Style="{StaticResource TitleTextStyle}"/>

                                                    <Border Grid.Column="1">
                                                        <Border.Style>
                                                            <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeOpenStyle}">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding open}" Value="False">
                                                                        <Setter Property="Background" Value="{DynamicResource StatusBadTint}"/>
                                                                        <Setter Property="BorderBrush" Value="{DynamicResource StatusBadEdge}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding StatusText}" FontSize="9" FontWeight="Bold">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{DynamicResource StatusGood}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding open}" Value="False">
                                                                            <Setter Property="Foreground" Value="{DynamicResource StatusBad}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>
                                                </Grid>

                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0"
                                                               Text="{Binding PlayerCountText}"
                                                               Style="{StaticResource InfoTextStyle}"/>

                                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding BlueScore}"
                                                                   Foreground="{DynamicResource TeamBlue}"
                                                                   FontSize="10"
                                                                   FontFamily="Consolas"
                                                                   FontWeight="Bold"/>
                                                        <TextBlock Text=" - "
                                                                   Foreground="{DynamicResource TextFaint}"
                                                                   FontSize="10"
                                                                   FontFamily="Consolas"/>
                                                        <TextBlock Text="{Binding OrangeScore}"
                                                                   Foreground="{DynamicResource TeamOrange}"
                                                                   FontSize="10"
                                                                   FontFamily="Consolas"
                                                                   FontWeight="Bold"/>
                                                    </StackPanel>
                                                </Grid>

                                                <Button Grid.Row="2"
                                                        Content="Spectate"
                                                        Click="QuickJoinButton_Click"
                                                        HorizontalAlignment="Right"
                                                        Style="{StaticResource SecondaryButtonStyle}"
                                                        Padding="12,4"
                                                        Margin="0,8,0,0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Border Grid.Column="1"
                        Background="{DynamicResource SurfaceCard}"
                        CornerRadius="8"
                        BorderBrush="{DynamicResource SurfaceBorder}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                CornerRadius="8,8,0,0"
                                Padding="14,10"
                                BorderBrush="{DynamicResource SurfaceBorder}"
                                BorderThickness="0,0,0,1">
                            <TextBlock Text="Server Details" Style="{StaticResource CardHeading}"/>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <Border Padding="16,20">
                                <StackPanel x:Name="ServerDetailsPanel">
                                    <TextBlock Text="Select a server to view details"
                                               FontSize="12"
                                               Foreground="{DynamicResource TextFaint}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="0,100,0,0"/>
                                </StackPanel>
                            </Border>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    Background="{DynamicResource SurfaceCard}"
                    CornerRadius="8"
                    Padding="14,9"
                    BorderBrush="{DynamicResource SurfaceBorder}"
                    BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="UpdateLabel"
                               Text="Last update: Never"
                               FontSize="10"
                               Foreground="{DynamicResource TextFaint}"
                               VerticalAlignment="Center"/>

                    <TextBlock x:Name="ApiStatusLabel"
                               Grid.Column="1"
                               Text="API: Disconnected"
                               FontSize="10"
                               Foreground="{DynamicResource StatusBad}"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
